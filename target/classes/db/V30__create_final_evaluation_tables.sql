WHENEVER SQLERROR EXIT FAILURE ROLLBACK
ALTER SESSION SET CURRENT_SCHEMA = &&APP_DB_SCHEMA;

CREATE TABLE GDR_EVALUACION_FINAL (
    ID_FINAL_EVALUATION NUMBER(19) NOT NULL,
    ID_ASSIGNMENT NUMBER(19) NOT NULL,
    CONSOLIDATED_SCORE NUMBER(18,4) DEFAULT 0 NOT NULL,
    EVALUATION_COMMENT VARCHAR2(1000 CHAR),
    STATUS VARCHAR2(20 CHAR) DEFAULT 'ACTIVE' NOT NULL,
    CREATED_AT TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL
);

CREATE TABLE GDR_DETALLE_PUNTAJE (
    ID_SCORE_DETAIL NUMBER(19) NOT NULL,
    ID_FINAL_EVALUATION NUMBER(19) NOT NULL,
    ID_GOAL NUMBER(19) NOT NULL,
    ACHIEVED_VALUE NUMBER(18,4) NOT NULL,
    SCORE_VALUE NUMBER(18,4) NOT NULL,
    DETAIL_COMMENT VARCHAR2(500 CHAR),
    CREATED_AT TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL,
    UPDATED_AT TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL
);
