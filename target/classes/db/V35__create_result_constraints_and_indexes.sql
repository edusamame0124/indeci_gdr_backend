WHENEVER SQLERROR EXIT FAILURE ROLLBACK
ALTER SESSION SET CURRENT_SCHEMA = &&APP_DB_SCHEMA;

ALTER TABLE GDR_RESULTADO
    ADD CONSTRAINT PK_GDR_RESULTADO PRIMARY KEY (ID_RESULT);

ALTER TABLE GDR_RESULTADO
    ADD CONSTRAINT UK_GDR_RESULTADO_ASIGNACION UNIQUE (ID_ASSIGNMENT);

ALTER TABLE GDR_RESULTADO
    ADD CONSTRAINT UK_GDR_RESULTADO_EVALUACION UNIQUE (ID_FINAL_EVALUATION);

ALTER TABLE GDR_RESULTADO
    ADD CONSTRAINT FK_GDR_RESULT_ASSIGNMENT
    FOREIGN KEY (ID_ASSIGNMENT) REFERENCES GDR_EVALUATION_ASSIGNMENT (ID_ASSIGNMENT);

ALTER TABLE GDR_RESULTADO
    ADD CONSTRAINT FK_GDR_RESULT_FINAL_EVALUATION
    FOREIGN KEY (ID_FINAL_EVALUATION) REFERENCES GDR_EVALUACION_FINAL (ID_FINAL_EVALUATION);

ALTER TABLE GDR_RESULTADO
    ADD CONSTRAINT CK_GDR_RESULT_STATUS
    CHECK (STATUS IN ('ACTIVE', 'INACTIVE'));

CREATE INDEX IX_GDR_RESULTADO_ASIGNACION ON GDR_RESULTADO (ID_ASSIGNMENT);
CREATE INDEX IX_GDR_RESULTADO_EVALUACION ON GDR_RESULTADO (ID_FINAL_EVALUATION);
CREATE INDEX IX_GDR_RESULTADO_ESTADO ON GDR_RESULTADO (STATUS);
