WHENEVER SQLERROR EXIT FAILURE ROLLBACK
ALTER SESSION SET CURRENT_SCHEMA = &&APP_DB_SCHEMA;

ALTER TABLE GDR_EVALUACION_FINAL
    ADD CONSTRAINT PK_GDR_EVALUACION_FINAL PRIMARY KEY (ID_FINAL_EVALUATION);

ALTER TABLE GDR_DETALLE_PUNTAJE
    ADD CONSTRAINT PK_GDR_DETALLE_PUNTAJE PRIMARY KEY (ID_SCORE_DETAIL);

ALTER TABLE GDR_EVALUACION_FINAL
    ADD CONSTRAINT UK_GDR_FINAL_EVALUATION_ASSIGNMENT UNIQUE (ID_ASSIGNMENT);

ALTER TABLE GDR_DETALLE_PUNTAJE
    ADD CONSTRAINT UK_GDR_SCORE_DETAIL_EVALUATION_GOAL UNIQUE (ID_FINAL_EVALUATION, ID_GOAL);

ALTER TABLE GDR_EVALUACION_FINAL
    ADD CONSTRAINT FK_GDR_FINAL_EVALUATION_ASSIGNMENT
    FOREIGN KEY (ID_ASSIGNMENT) REFERENCES GDR_EVALUATION_ASSIGNMENT (ID_ASSIGNMENT);

ALTER TABLE GDR_DETALLE_PUNTAJE
    ADD CONSTRAINT FK_GDR_SCORE_DETAIL_EVALUATION
    FOREIGN KEY (ID_FINAL_EVALUATION) REFERENCES GDR_EVALUACION_FINAL (ID_FINAL_EVALUATION);

ALTER TABLE GDR_DETALLE_PUNTAJE
    ADD CONSTRAINT FK_GDR_SCORE_DETAIL_GOAL
    FOREIGN KEY (ID_GOAL) REFERENCES GDR_GOAL (ID_GOAL);

ALTER TABLE GDR_EVALUACION_FINAL
    ADD CONSTRAINT CK_GDR_FINAL_EVALUATION_STATUS
    CHECK (STATUS IN ('ACTIVE', 'INACTIVE'));

ALTER TABLE GDR_DETALLE_PUNTAJE
    ADD CONSTRAINT CK_GDR_SCORE_DETAIL_ACHIEVED
    CHECK (ACHIEVED_VALUE >= 0);

ALTER TABLE GDR_DETALLE_PUNTAJE
    ADD CONSTRAINT CK_GDR_SCORE_DETAIL_SCORE
    CHECK (SCORE_VALUE >= 0);

CREATE INDEX IX_GDR_EVALUACION_FINAL_ASIGNACION ON GDR_EVALUACION_FINAL (ID_ASSIGNMENT);
CREATE INDEX IX_GDR_EVALUACION_FINAL_ESTADO ON GDR_EVALUACION_FINAL (STATUS);
CREATE INDEX IX_GDR_DETALLE_PUNTAJE_EVALUACION ON GDR_DETALLE_PUNTAJE (ID_FINAL_EVALUATION);
CREATE INDEX IX_GDR_DETALLE_PUNTAJE_META ON GDR_DETALLE_PUNTAJE (ID_GOAL);
