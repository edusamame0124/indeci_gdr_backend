WHENEVER SQLERROR EXIT FAILURE ROLLBACK
ALTER SESSION SET CURRENT_SCHEMA = &&APP_DB_SCHEMA;

CREATE SEQUENCE SQ_GDR_EVAL_QUAL_EMAIL_LOG START WITH 1 INCREMENT BY 1 NOCACHE NOCYCLE;

CREATE TABLE GDR_EVAL_QUAL_EMAIL_LOG (
    ID_EVAL_QUAL_EMAIL_LOG NUMBER(19) NOT NULL,
    ID_FINAL_EVALUATION NUMBER(19) NOT NULL,
    FECHA_ENVIO TIMESTAMP(6) DEFAULT SYSTIMESTAMP NOT NULL,
    DESTINATARIO VARCHAR2(254 CHAR) NOT NULL,
    CODIGO_PLANTILLA VARCHAR2(40 CHAR),
    ASUNTO VARCHAR2(500 CHAR),
    ESTADO VARCHAR2(20 CHAR) NOT NULL,
    MENSAJE_ERROR VARCHAR2(1000 CHAR),
    ID_USUARIO_ACCION NUMBER(19) NOT NULL
);

ALTER TABLE GDR_EVAL_QUAL_EMAIL_LOG
    ADD CONSTRAINT PK_GDR_EVAL_QUAL_EMAIL_LOG PRIMARY KEY (ID_EVAL_QUAL_EMAIL_LOG);

ALTER TABLE GDR_EVAL_QUAL_EMAIL_LOG
    ADD CONSTRAINT FK_GDR_EVAL_QUAL_LOG_FINAL_EVAL
    FOREIGN KEY (ID_FINAL_EVALUATION) REFERENCES GDR_EVALUACION_FINAL (ID_FINAL_EVALUATION);

ALTER TABLE GDR_EVAL_QUAL_EMAIL_LOG
    ADD CONSTRAINT FK_GDR_EVAL_QUAL_LOG_ACCION_USER
    FOREIGN KEY (ID_USUARIO_ACCION) REFERENCES SEC_USER (ID_USER);

ALTER TABLE GDR_EVAL_QUAL_EMAIL_LOG
    ADD CONSTRAINT CK_GDR_EVAL_QUAL_LOG_ESTADO
    CHECK (ESTADO IN ('ENVIADO', 'FALLIDO'));

ALTER TABLE GDR_EVAL_QUAL_EMAIL_LOG
    ADD CONSTRAINT CK_GDR_EVAL_QUAL_LOG_ASUNTO_O_PLANTILLA
    CHECK (ASUNTO IS NOT NULL OR CODIGO_PLANTILLA IS NOT NULL);

CREATE INDEX IX_GDR_EVAL_QUAL_LOG_FINAL_EVAL ON GDR_EVAL_QUAL_EMAIL_LOG (ID_FINAL_EVALUATION);
CREATE INDEX IX_GDR_EVAL_QUAL_LOG_FECHA_ENVIO ON GDR_EVAL_QUAL_EMAIL_LOG (FECHA_ENVIO);
CREATE INDEX IX_GDR_EVAL_QUAL_LOG_USUARIO ON GDR_EVAL_QUAL_EMAIL_LOG (ID_USUARIO_ACCION);
